# test.yaml
dataset: "phylogenetic_gmm"
mode: "adapt"
num_seeds: 1
starting_seed: 42
n_trains: [100]
n_pools: [100]
shifts: [1.0]
dim: 1280
n_families: 20
n_test: 100
batch_size: 16
base_epochs: 2
learning_rate: 0.001
hidden_dim: 128
dropout: 0.1
